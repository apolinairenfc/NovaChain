import React from 'react';
declare type SliderType = 'slider' | 'range';
declare type SliderValue<T extends SliderType> = T extends 'slider' ? number : [number, number];
export declare type CustomMarkType<T extends 'slider' | 'range'> = React.ComponentType<{
    stepMarked: boolean;
    currentValue: SliderValue<T>;
    index: number;
    min: number;
    max: number;
    markValue: number;
}>;
declare type CustomMarksProps<T extends 'slider' | 'range'> = {
    StepMarker?: CustomMarkType<T>;
    minimumValue: number;
    maximumValue: number;
    step: number;
    activeValue: SliderValue<T>;
    type: T;
    inverted: boolean;
    vertical: boolean;
};
declare const Marks: <T extends "range" | "slider">(props: CustomMarksProps<T>) => JSX.Element;
export default Marks;
